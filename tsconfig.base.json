{
  "//": "Shared TypeScript compiler options for workspace packages under packages/*. apps/web/tsconfig.json is intentionally NOT extending this (different target + jsx) and carries its own @ft/* path aliases. Update both when adding a new @ft/* package.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "incremental": true
  }
}
