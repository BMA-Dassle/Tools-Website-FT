"use client";

/**
 * Theme hook for the daily-events board — the reservations board's
 * ?theme= + portal postMessage sync, shared as-is.
 */
export { useBoardTheme } from "~/features/reservations-admin/hooks";
