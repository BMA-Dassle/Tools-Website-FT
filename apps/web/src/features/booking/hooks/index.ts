/**
 * React Query hooks for booking — feature-scoped.
 *
 * Each activity owns its own hooks (useAvailability, useDraft, usePayment,
 * useConfirmation) that read via the data layer and key into `bookingKeys`.
 *
 * PR-B1 ships the directory; real hooks land per-activity in PR-B2..B6.
 */
export {};
