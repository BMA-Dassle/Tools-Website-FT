/** `~/features/crm/kpi` — DDL only in PR1; C7 adds `service/{accountability,kpi,pacing}.ts`. */
export { ensureTargetsSchema } from "./data/targets-db";
export { ensureGoalsSchema } from "./data/goals-db";
