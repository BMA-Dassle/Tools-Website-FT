/** `~/features/crm/cold` — DDL only in PR1; the cold-list PR adds `service/import.ts`. */
export { ensureColdListsSchema } from "./data/lists-db";
export { ensureColdRowsSchema } from "./data/rows-db";
