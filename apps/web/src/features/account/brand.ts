import type { BrandKey } from "./types";

/** Per-brand accent applied to account controls via the `--account-accent` CSS var. */
export const BRAND_ACCENT: Record<BrandKey, string> = {
  fasttrax: "#00e2e5",
  headpinz: "#fd5b56",
};
