{
  "name": "vt3-bridge",
  "version": "0.1.0",
  "description": "SSE→webhook bridge for VT3 video events. Holds the long-lived stream Vercel can't.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "probe": "PROBE=1 tsx src/index.ts"
  },
  "dependencies": {
    "undici": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
