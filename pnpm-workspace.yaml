# pnpm workspace globs.
# - fasttrax-web stays at root for now (PR3 moves it to apps/web).
# - Bridges stay at root for now (Phase 3 moves them to apps/).
# - packages/* and apps/* are populated in later PRs but are valid empty globs today.
packages:
  - "fasttrax-web"
  - "kart-timing-bridge"
  - "vt3-bridge"
  - "packages/*"
  - "apps/*"
