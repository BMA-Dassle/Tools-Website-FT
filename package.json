{
  "name": "tools-website-ft",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@11.6.4",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "fasttrax-web",
    "kart-timing-bridge",
    "vt3-bridge",
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
